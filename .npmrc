# React Native + Expo require a flat, hoisted node_modules. pnpm's default
# symlinked layout breaks Metro resolution and native (Gradle/CocoaPods) builds,
# so we opt into an npm-like hoisted layout.
node-linker=hoisted
# Expo's autolinking and some RN deps reach for hoisted peers; expose them.
shamefully-hoist=true
