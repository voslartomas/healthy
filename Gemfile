source 'https://rubygems.org'

# fastlane drives store-listing uploads (metadata + screenshots) for Healthy.
# The app binaries themselves are built and submitted by EAS (see package.json
# `build-ios` / `submit-ios` etc.); fastlane only pushes the App Store Connect
# and Google Play *store assets* that live in version control under fastlane/.
gem 'fastlane', '~> 2.228'
